#!/usr/bin/env bash
set -euo pipefail
REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
exec "${REPO_ROOT}/tools/portable/service/linux_amd64/rogue-mysql-server/rogue_mysql_server" "$@"
