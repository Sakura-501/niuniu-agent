#!/usr/bin/env bash
set -euo pipefail
REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
SCRIPT_DIR="${REPO_ROOT}/tools/portable/service/linux_amd64/redis-rogue-server"
cd "${SCRIPT_DIR}"
exec uv run python "${SCRIPT_DIR}/redis-rogue-server.py" "$@"
